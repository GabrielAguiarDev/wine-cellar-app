# IL DiVino — Adega Prime · Contexto de Desenvolvimento

> Arquivo mantido pelo agente **mobile-senior-dev**. Leia isto antes de continuar o desenvolvimento.
> **Última atualização: FAQ em acordeão no Perfil + `/payment-methods` com cadastro de cartão em flip card — 2026-07-30**
> Fonte de design em uso: pasta **`design/`** → `design/project/IL DiVino.dc.html` (export Claude Design). É a fonte da verdade visual; reconstruir pixel-perfect, sem copiar a estrutura interna do protótipo.

---

## 1. Visão geral

App mobile de **adega premium (e-commerce de vinhos)** — catálogo curado, quiz de paladar, sommelier virtual por ocasião, busca por vinho/prato, sacola, checkout com gifting, acompanhamento de pedido, favoritos, perfil, fidelidade e acesso VIP. Estética de marca: **luxo sóbrio** (creme + bordô + dourado, tipografia serif). Público: consumidores de vinho de ticket alto. 15 telas + tab bar.

## 2. Stack & decisões

- **Expo SDK 57** (managed) + **Expo Router** (file-based) + **TypeScript strict**. React Native 0.86 / React 19.2.3 / Node ≥22.13.
- **Estilização: `@shopify/restyle`** (tema tipado). Decisão do usuário. Substitui o `StyleSheet` do plano original.
- **Estado: `zustand` agora** (carrinho/favoritos/quiz/pontos são mock) → **migrar para `@tanstack/react-query` + Context na Fase 16** (backend real). Decisão do usuário. **`useUserStore` é persistido** via `zustand/middleware persist` + `@react-native-async-storage/async-storage` (flag `onboarded` → quiz só no 1º acesso; `paladar`/`points`).
- **`@expo/ui` usado de forma PONTUAL** (decisão do usuário: "pontual"). Adotados os drop-ins cross-platform `@expo/ui/community/*` onde o controle nativo é superior e neutro à marca: **`SegmentedControl`** (pagamento no checkout; toggle vinho/prato na busca) e **`DateTimePicker`** (data do presente). A UI de marca (cards, botões, headers custom, `Toggle`) permanece em RN + restyle — o `Switch` nativo não expõe cor e viraria verde de sistema, então o `Toggle` bordô foi mantido.
- **Tema de marca fixo** (NÃO dirigido pelo modo claro/escuro do SO): o design define fundo claro vs. escuro **por tela** (ex.: splash/quiz/produto premium são dark; home/busca são creme), não por preferência do sistema. Por isso há um único `theme` com tokens claros e escuros disponíveis simultaneamente.
- **Splash animada = Lottie** (`assets/lottie/wine.json`, ~5,4s/60fps, vetor puro). Fluxo: splash nativo **só cor sólida bordô** (sem logo) → ao carregar o JS, overlay `AnimatedSplash` (mesma cor, sem flash) toca o Lottie 1x em tela cheia (**sem texto/logo**) via `lottie-react-native` → **fade out** (reanimated) revela o app. Decisão do usuário: o Lottie **é** a splash (não há tela de marca "Entrar"; o fluxo do design que tinha Splash→Quiz vira **AnimatedSplash→Quiz**). `speed` ~1.8x (~3s) — ajustável em `AnimatedSplash.tsx`.
- Gradientes via **`expo-linear-gradient`**; ícones e garrafas via **`react-native-svg`** (garrafas serão desenhadas com Views/SVG, não imagens). Animações: **`react-native-reanimated` 4** (plugin `react-native-worklets/plugin` no babel). Gestos: **`react-native-gesture-handler`**. Fontes: **`@expo-google-fonts/cormorant-garamond` + `@expo-google-fonts/jost`** via `expo-font`. Env validado com **`zod`**.
- **Desvio consciente da persona**: a arquitetura padrão dela é *RN bare + react-navigation + yarn*; aqui o usuário pediu explicitamente **Expo + Expo Router** (npm). Mantidos: TS strict, restyle, aliases `@`, domínio Api/Adapter/Service/useCases (Fase 16), pt-BR, barrels.

## 3. Estrutura & convenções específicas deste projeto

- **Rotas em `app/`** (raiz — padrão Expo Router). **Todo o resto em `src/`.**
```
app/                      # rotas expo-router
  _layout.tsx             # splash hold + providers + Stack (headerShown:false)
  index.tsx               # placeholder Fase 0 (vira Splash na Fase 4)
src/
  components/             # design system + AppProviders  (Box, Text, ...)
  theme/                  # palette.ts (bruta+alpha) · theme.ts (restyle) · index.ts
  config/                 # env.ts + env.schema.ts (zod)
  hooks/                  # useAppTheme, useAppFonts
  data/ domain/ services/ store/ utils/   # (vazios — próximas fases)
assets/                   # fonts/images/lottie (mantidos na raiz)
```
- **Path aliases** (tsconfig, resolvidos pelo Metro do Expo): `@/*`→`src/*`, `@components`, `@theme`, `@config`, `@data`, `@domain`, `@services`, `@store`, `@hooks`, `@utils`, `@assets`. Sem `baseUrl` (deprecado no TS 6).
- **restyle**: usar `Box`/`TouchableOpacityBox`/`PressableBox` e `Text` (variantes). Props de tema (`padding="s22"`, `backgroundColor="surface"`). `spacing` = `sNN` em px; `borderRadii` = `rNN`; cores **sempre semânticas** (nunca hex direto — a paleta bruta e os rgba estão mapeados em `theme.colors`).
- **Fontes** (chaves registradas): serif `CormorantGaramond_{400,500,600,700}[_Italic]`; sans `Jost_{300,400,500,600}`. Aliases em `theme/theme.ts → fonts`.
- **Testes**: `jest` puro (config em `jest.config.js`, `babel-jest` + aliases; **sem** preset jest-expo pois testamos lógica pura). Co-localizados em `__tests__/`, importam globals de `@jest/globals`. Rodar: `npm test`. Typecheck: `npm run typecheck`.
- Componentes: **functional + named export** (`export function Nome`), sem default (exceto telas expo-router, que exigem default export). Prettier: aspas simples, `trailingComma:all`, `arrowParens:avoid`, `bracketSameLine:true`. ESLint flat (`eslint-config-expo`) + `import/order`. **Strings de UI em pt-BR.**

## 4. Mapa de progresso (fases)

- [x] **Fase 0 — Fundação** — Expo+Router reestruturado p/ `src/`, deps, aliases, env(zod), **tema restyle** (palette+semantic+textVariants+spacing+radii), fontes carregando com splash hold, providers, primitivos `Box`/`Text`, tela placeholder. **Typecheck limpo + `expo export` iOS OK.**
- [x] **Fase 1 — Design System & primitivos** — `Icon`(12 ícones SVG), `StarRating`, `BottleGraphic`(+premium/full), `Button`(primary/outline/outlineGold), `Pill`, `Chip`, `SectionTitle`, `WineCard`, `WineRow`(claro/escuro), `SegmentedToggle`, `Toggle`, `Toast`, `ScreenHeader`, `Screen`(+gradiente/scroll/footer). Catálogo temporário em `app/index.tsx`. **Typecheck+lint+export OK.**
- [x] **Fase 2 — Dados & estado** — data/ (10 vinhos, reviews, quiz, ocasiões), tipos, formatters (`brl`/`nf`), regras de preço, seletores (rails/busca/carrinho), view-model mappers, stores zustand (cart/favorites/user/toast). **27 testes (jest) + typecheck + lint OK.**
- [x] **Fase 3 — Navegação & shell** — Stack único (sem Tabs) + **TabBar flutuante custom como overlay** (visibilidade + aba ativa por rota + badge da sacola), Toast global (`ToastHost`+store), 15 rotas como stubs navegáveis (`DevStub`), catálogo do DS movido p/ `/catalog`, `index`→redirect `/quiz`. **typecheck+lint+export OK.**
- [x] **Fase 4 — Onboarding (Quiz)** — `app/quiz.tsx` real: 3 perguntas (`QUIZ`), progresso "x / N", opções label+hint, dots, "Pular"; grava `paladar` (do `corpo`) no `useUserStore` e `router.replace('/home')`. typecheck+lint+export OK.
- [x] **Fase 5 — Home** — `app/home.tsx` real: header (logo + coração→favoritos), busca fake→/search, banner "Curadoria da semana", pills de categoria→/search, rail "Selecionados" (`WineCard`+`railSelected`, favoritar), card "Coleção reservada"→especiais, rail "Mais vendidos" (`WineRow`+`railBestSellers`), rodapé. Navega p/ `/product/[id]`. typecheck+lint+export OK.
- [x] **Fase 6 — Busca/Coleção** — `app/search.tsx` real: título dinâmico, `SegmentedToggle` vinho/prato, lê param `cat` da Home, `searchWines`/`searchByDish`, input (TextInput), chips de filtro (visuais), resultados `WineRow`, exemplos de prato, link Sommelier. typecheck+lint+export OK.
- [x] **Fase 7 — Produto (Padrão + Premium)** — `app/product/[id].tsx` real: escolhe layout por `destaque`. Premium (dark, nome+garrafa premium, rating→reviews, assinatura, vídeo play/pausa `useState`, harmoniza, estoque baixo, rodapé fixo Reservar/Adquirir) e Padrão (creme, garrafa central, infos, adquirir, harmoniza). `buyFromProduct` (reserva vs sacola+toast+ir p/ bag), favoritar. typecheck+lint+export OK.
- [x] **Fase 8 — Avaliações** — `app/reviews/[id].tsx` real: header com nome, média destaque (nota 64 + `StarRating` + total), form (estrelas editáveis + `TextInput` comentário + enviar→toast+back), lista `REVIEWS` (nome + glifos ★ + comentário itálico opcional). typecheck+lint+export OK.
- [x] **Fase 9 — Sommelier** — `app/sommelier.tsx` real (dark): "Qual é a ocasião?", grade 2×2 (`OCCASIONS`) selecionável, lista de vinhos da ocasião (`winesByIds` + `WineRow` dark)→produto. typecheck+lint+export OK.
- [x] **Fase 10 — Favoritos** — `app/favorites.tsx` real: lista `WINES.filter(favs)` em `WineRow` com `rightSlot` (coração remove + "+" adiciona à sacola+toast), estado vazio, →produto. typecheck+lint+export OK.
- [x] **Fase 11 — Sacola** — `app/bag.tsx` real: vazio (ícone+"Explorar vinhos"), itens com stepper `setQty`±1 (remove em 0) + subtotal, rail "Combina com sua compra" (sugestões), resumo (`cartCount`/`cartSubtotal`) + "Finalizar compra"→/checkout. typecheck+lint+export OK.
- [x] **Fase 12 — Checkout + Gifting** — `app/checkout.tsx` real: endereço mock, pagamento (Pix/Cartão/Boleto), `Toggle` pontos, gifting expansível (mensagem+ocultar preço+data), resumo (`pointsDiscount`/`frete`/`checkoutTotal`), "Confirmar pedido"→`clear()`+`/tracking`. typecheck+lint+export OK.
- [x] **Fase 13 — Acompanhamento de pedido** — `app/tracking.tsx` real: toggle Status/Mapa; timeline de 4 etapas (feito/atual/futuro) com dots+linhas coloridos; mapa estilizado (rota SVG tracejada + pino + entregador) + card do entregador com botão telefone. typecheck+lint+export OK.
- [x] **Fase 14 — Perfil + Fidelidade + VIP** — `profile.tsx` (card VIP+pontos/progresso, acesso antecipado, paladar do `useUserStore`, pedidos recentes, links), `loyalty.tsx` (hero pontos, ganhar, resgatar+toast, histórico), `vip.tsx` (dark, `especiais()` em `WineRow` dark badge "Pré-lançamento"). **`DevStub` removido — não há mais stubs.** typecheck+lint+export OK.
- [x] **Fase 15 — Polimento** — reanimated: `Blip` (entregador/estoque), `PulseBar` (vídeo), fade-in de tela (`Screen`), dots do quiz (`LinearTransition`), expansão do gifting (`FadeInDown`); teclado (`keyboardShouldPersistTaps`/`on-drag`); a11y labels. typecheck+lint+27 testes+export OK.
- [ ] Fase 16 — Integração backend (react-query; substitui mocks) — opcional/futuro

### Refinamento nativo (pós-MVP, 2026-07-24) — Fases A–F

Objetivo do usuário: parecer/comportar-se como app nativo iOS/Android **sem perder a identidade** (bordô/creme/serif). Todas estabilizadas (tsc+eslint+27 testes+export iOS/Android).

- [x] **Fase A — Ajustes rápidos** — removido o fade-in de troca de tela (`Screen` sem `FadeIn`; `Stack` sem `animation:'fade'`); splash com fundo **creme** (não bordô) em `app.json` + `AnimatedSplash`; `StatusBar` global `dark` (+ `light` local em quiz/produto premium/sommelier/vip); removido o botão de menu (drawer) do header da Home.
- [x] **Fase B — Onboarding no 1º acesso** — `@react-native-async-storage/async-storage` + `persist`; `useUserStore.onboarded`/`completeOnboarding()`; `app/index.tsx` aguarda hidratação e redireciona (`onboarded ? /home : /quiz`); `quiz.finish()` marca onboarded (inclui "Pular").
- [x] **Fase C — Navegação (tabs) + Native Tabs** — grupo `app/(tabs)/` com pilhas aninhadas por aba. **iOS:** `expo-router/unstable-native-tabs` (SF Symbols, labels pt-BR, `tintColor` bordô, badge da sacola). **Android:** `<Stack>` + a `TabBar` flutuante custom (overlay do root, agora só fora do iOS). `sommelier/loyalty/vip` saíram do `VISIBLE_ON` → **tela cheia**.
- [x] **Fase D — Headers/title nativos** — `src/theme/navHeader.ts` (`brandHeaderOptions`: bg creme, tinta bordô, título Cormorant, `headerLargeTitle` iOS); `Screen` ganhou prop **`nativeHeader`** (sem inset manual + `contentInsetAdjustmentBehavior="automatic"`). Header nativo em: **abas** search/favorites/bag/profile e **pushes** checkout/reviews/loyalty (voltar nativo). Telas imersivas escuras (sommelier/vip/produto premium/quiz) mantêm título artístico custom.
- [x] **Fase E — Expo UI pontual** — `@expo/ui@57.0.7`; `community/segmented-control` (checkout + busca) e `community/datetime-picker` (data do presente, compact no iOS / diálogo no Android). `Toggle` custom mantido.
- [x] **Fase F — Estabilização + docs** — bateria completa verde; este documento atualizado.

### Central de notificações (2026-07-28)

- [x] **Ícone do header da Home**: o **coração** (que duplicava a aba Favoritos) saiu; no lugar entrou **`bell`** com badge de não lidas → **`/notifications`**. Favoritos continua acessível pela tab bar.
- [x] **`app/notifications.tsx`** — push da Stack raiz (tela cheia, sem tab bar) com o **`AnimatedHeaderScrollView`** (mesmo large title colapsável das abas) + **`ScreenHeader` (‹ Voltar) no `leftComponent`**. Voltar cai na Home quando a tela é a primeira da pilha (`router.canGoBack()` falso — deep link / futura push). Feed agrupado em Hoje / Esta semana / Anteriores, linha com ícone por tipo (curadoria/pedido/favorito/fidelidade/vip), rótulo + tempo relativo, título serifado, corpo e **ponto bordô de não lida**; "Marcar todas como lidas" no topo. Tocar marca como lida e navega.
- [x] **`app/loyalty.tsx` padronizada** (pedido do usuário) — mesmo header de `/notifications`; era header nativo, que no iOS 26 aparecia sem título grande e com o conteúdo passando por baixo da barra sem fundo/blur.
- [x] **`src/hooks/useGoBack.ts`** — voltar à prova de pilha vazia (`canGoBack()` falso → rota de fallback). Usado por `/notifications` (→ `/home`) e `/loyalty` (→ `/profile`).
- [x] **Dados/estado**: `src/data/notifications.ts` (`NOTIFICATIONS` + seletores `unreadNotifications`/`unreadNotificationCount`/`notificationsByGroup`) e `src/store/useNotificationsStore.ts` (mapa `read` + `markRead`/`markAllRead`/`isRead`, semente = itens `read: true` do mock). **Não persistido** (só `useUserStore` é). Ícones novos no DS: `bell`, `gift`. **7 testes novos (34 no total) + typecheck + lint + export iOS OK.**
- **⚠️ O header nativo com large title não desenha título no iOS 26.** A tela nasceu com `brandHeaderOptions` + `Screen nativeHeader` (padrão da Fase D) e no simulador iOS 26.0 aparecia **só o botão voltar**: nem título grande, nem título compacto ao rolar, com a faixa do header ocupando espaço vazio. Verificado por deep link que **`/loyalty` tem o mesmo sintoma** — ou seja, atinge também checkout/reviews, não é da tela nova. É a armadilha já documentada em `theme/navHeader.ts` (o `react-native-screens` precisa ACHAR o ScrollView da tela). Por isso esta tela usa título em JS. **`/loyalty` já foi migrada** para o header em JS (mesmo do `/notifications`). **Pendência:** `checkout.tsx` e `reviews/[id].tsx` são as últimas com `brandHeaderOptions` + `Screen nativeHeader` — migrar do mesmo jeito ou investigar a detecção do scroll no RNS 4.26. Ao migrar por Fast Refresh, **relançar o app**: as opções de header já aplicadas à rota não são revertidas pelo hot reload e o botão voltar nativo continua desenhado por cima (foi visto uma vez e sumiu no relaunch).
- **Destino de cada notificação é de DOMÍNIO, não de rota** (`NotificationTarget` → a tela traduz para `router.navigate`). De propósito **não existe destino `/curation/[id]`**: é o outro estado do card da Home (shared element com `animation: 'none'`) e, sem origem medida, apareceria de salto — as notificações de curadoria levam à coleção reservada (`/reserved`, ver seção abaixo; era `/search?cat=CAT_SPECIALS`), que tem push normal.

### Atalhos por país na Home (2026-07-29)

- [x] **"Explorar por tipo" → "Explorar por país"** (pedido do usuário). O rail agora leva à busca com o filtro **`country`** (`/search?country=Itália`), não mais `cat`/tipo. `WineTypeCard` **removido** (ficaria sem uso; está no git se voltar a fazer falta) — no lugar, **`WineCountryCard`**: bandeira ocupando o card inteiro (152×98), véu bordô em gradiente por cima (contraste do nome + segura a saturação das cores nacionais dentro da paleta sóbria) e nome serifado + contagem no rodapé do card.
- [x] **`src/components/Flag.tsx`** — bandeira em **SVG desenhado** (faixas com peso opcional + emblema), sem asset/imagem: `viewBox` 3×2 com `preserveAspectRatio="none"`, então estica junto com o card. Mapeados **Itália, França, Portugal, Argentina (sol), Espanha**; país fora do mapa cai num campo bordô liso (o card segue legível). As cores nacionais ficam **fora do tema** de propósito — não são tokens de marca.
- [x] **`countriesWithCount()`** (`@data/selectors`) — países do catálogo com contagem, do mais representado ao menos. A lista **sai do catálogo** (via `wineCountry`), então país novo em `REGION_COUNTRY` aparece sozinho no rail. Hoje: Itália 5 · França 4 · Argentina 1.
- [x] **`/search` lê `cat` E `country`** (`paramFilters`), e o resync em render passou a comparar os dois params (a busca é aba: pode já estar montada quando a Home navega). **3 testes novos (54 no total) + typecheck + eslint + export iOS OK.**

### Coleção reservada — `/reserved` (2026-07-29)

Pedido do usuário: "para essa tela onde eu irei mostrar os vinhos raros e especiais quero uma tela bem apresentável… me surpreenda". O card **"Vinhos raros & especiais"** da Home caía em `/search?cat=__specials` — três garrafas de edição limitada apresentadas como resultado de busca, com chips de filtro por cima. Agora tem tela própria.

- [x] **`app/reserved.tsx`** — push da Stack raiz (tela cheia, sem tab bar), **animação de card padrão**. Ao contrário de `/curation/[id]` aqui **não há shared element**: a origem é um banner de gradiente e o destino abre numa fotografia, não existe forma comum para morfar — então não se declara nada no `app/_layout.tsx`. Status bar `light`, foto sangrando por baixo dela; padding inferior = `insets.bottom` (não o `s108` das abas).
- [x] **`src/components/ParallaxHeaderScrollView/`** (`index.tsx` + `conf.ts` + `types.ts`) — porte do **[parallax-header do reacticx](https://www.reacticx.com/docs/components/parallax-header)**, irmão do `AnimatedHeaderScrollView`. Lá o herói é o TÍTULO; aqui é uma **fotografia** (`assets/images/banner-loja.png`, a adega da própria loja) e o título vive por cima dela. Quatro camadas animam: **(1)** a foto rola a `PARALLAX_FACTOR` 0.5 — como ela é o 1º filho do ScrollView já sobe a 1.0, então "meia velocidade" é conseguido EMPURRANDO-A de volta para baixo em metade do offset, e o conteúdo desliza por cima; **(2)** em overscroll ela cresce `1 + d/H` e sobe `d/2`, o que mantém a borda de cima colada na status bar (o crescimento não é enfeite — é o que fecha o topo, e é por isso que a prop `disableScale` do original **não** foi portada); **(3)** o `overlay` (eyebrow/título/chamada) sai em fade em `[0.25H, 0.7H]` — começar em 0 fazia o título sumir no meio da tela sem motivo, e terminar em 0.7 garante que ele nunca coexista com o título compacto; **(4)** barra fixa com gradiente + blur + `MaskedView`/`easeGradient` (mesma borda difusa do `AnimatedHeaderScrollView`), título compacto entrando depois, e `leftComponent`/`rightComponent` **sempre visíveis** — sobre uma foto o voltar não pode nascer invisível. Reanimated 4 no lugar do `Animated` da RN do original (misturar as duas APIs no mesmo scroll é pedir jank).
- [x] **`src/components/RareWineCard.tsx`** — ficha editorial do rótulo. **Não é um `WineRow` maior**: a linha existe para varrer lista, e aqui há três garrafas que precisam ser lidas uma a uma. Nome em largura cheia (27), assinatura do sommelier por extenso em serif itálico, dados técnicos em **`SpecChip`** (valor serifado grande + rótulo miúdo — o idioma "13,3% / Alcohol" da referência que o usuário mandou), pill "Últimas garrafas" com `Blip` quando `lowStock`, e rodapé com o vídeo do sommelier + preço.
  - **Nicho retroiluminado**: a garrafa não fica sobre o card, fica dentro de uma reentrância escura (`videoBackdrop`) com gradiente dourado subindo da base e fita de luz (`goldA60`, 2px) no rodapé. É **citação direta da foto do hero** — as prateleiras da adega são iluminadas por baixo, e é o que faz fotografia e fichas parecerem o mesmo lugar. Luz em gradiente e **não** sombra colorida: no Android sombra colorida é irregular e cairia em três instâncias na mesma tela.
  - **⚠️ Armadilha do véu no overscroll (corrigida — não reintroduzir):** o scrim é `absoluteFill` da CAIXA do header, e no overscroll essa caixa desce `d` px junto com o conteúdo — mas a foto cresce e continua colada no topo da janela. Resultado: a faixa `[0, d]` mostrava a foto **crua** enquanto logo abaixo o gradiente já começava em 35% de escuro, e um **degrau horizontal nítido atravessava a fotografia** ao arrastar para baixo. A correção é a `View` `scrimOverscroll` — `top: -imageHeight`, altura de uma foto, pintada com a **primeira parada do gradiente**: encostada nela a emenda desaparece e a faixa revelada parece só mais um pedaço do topo da foto. Ao mexer no scrim, testar SEGURANDO o arrasto para baixo (soltar deixa o bounce voltar antes de dar para ver).
  - **Calibrações feitas no simulador (iOS 26):** numeração `01/02/03` em **sans**, não na serifada — o Cormorant usa algarismos de estilo antigo e `01` saía parecendo uma palavra (nos anos de safra o efeito é bem-vindo, em índice não); nicho **186×80 com garrafa 48** (era 202/52 — sobrava vazio à direita); linha nicho+texto com **`alignItems="center"`**, senão os rótulos de assinatura curta e sem "últimas garrafas" (Corona Reale) ficavam com um buraco embaixo do texto.
- [x] **`src/data/collections.ts`** — `RESERVED_COLLECTION` (eyebrow/title/**teaser** para o card da Home/**lead** para o hero/note/ctaLabel). Mesmo motivo de `CURATIONS`: o card da Home e a tela mostram o MESMO eyebrow e o MESMO título; duplicados nos dois arquivos, um dia divergem. O banner da Home foi religado a essa fonte.
- [x] **`specialsSummary()`** (`@data/selectors`) — contagem + faixa de safras + média das notas da coleção; alimenta a faixa de dados sob o hero (hoje: **3 rótulos · 2016–2019 · ★ 4,8**). Sai do catálogo, então marcar/desmarcar `featured` já move a faixa; `null` com coleção vazia (a tela esconde a faixa inteira). A média é **arredondada a 1 casa** no seletor — sem isso a soma de floats devolvia `4.800000000000001`.
- [x] **`toRareWineCardData()`** (`@utils/wineViewModel`) — leva a ficha inteira (assinatura, safra, avaliações, vídeo, estoque), ao contrário de `toWineCardData`/`toWineRowData`.
- [x] **Corpo escuro contínuo** (`primaryDeep`): o véu da foto termina exatamente em `palette.wineDeep`, que é o fundo do corpo — não existe emenda entre fotografia e conteúdo, a foto só escurece até virar tela. É também por isso que o corpo é **sólido e não gradiente**. O wrapper dos filhos **tem de ser opaco**: a foto rola a meia velocidade e o conteúdo passa por cima dela.
- [x] **`/reserved` é o novo destino de `specials`** no `switch` de `app/notifications.tsx` e do card da Home (`goReserved`). `CAT_SPECIALS` continua existindo como filtro da busca (opção "Especiais" no `FilterSheet`) — só deixou de ser destino de navegação.
- **3 testes novos (57 no total) + typecheck + eslint + export iOS/Android OK.** Validado no simulador iOS 26 (hero, parallax, barra colapsada, as 3 fichas, nota + CTA).

### Voltar redondo padronizado (2026-07-30)

Pedido do usuário depois de ver o botão do hero de `/reserved`: "nas telas onde só tem esse botão voltar adicione todos nesse mesmo padrão".

- [x] **`src/components/BackButton.tsx`** — círculo de 36 com borda e chevron centrado, em duas variantes: **`dark`** (vidro bordô `wineA60` + borda/seta douradas — a que nasceu sobre a fotografia de `/reserved`) e **`light`** (superfície creme + borda `inkBorder14` + seta bordô). **O padrão é a FORMA, não a cor**: vidro bordô sobre creme viraria um borrão escuro e a seta dourada perde contraste no claro.
- [x] **Aplicado nas 5 telas cujo topo tem SÓ o voltar:** `sommelier.tsx` e `vip.tsx` (variante `dark`), `notifications.tsx` e `loyalty.tsx` (variante `light`, dentro do `leftComponent` do `AnimatedHeaderScrollView`) e `tracking.tsx` (`light`). Em `/reserved` o botão inline foi trocado pelo componente.
- [x] **`tracking.tsx`**: o rótulo `"Início"` do `ScreenHeader` era a única pista de que o botão vai para a Home e não para a tela anterior — virou `accessibilityLabel="Início"`, para não perder a informação em leitor de tela.
- **`ScreenHeader` NÃO foi aposentado**: ele resolve outro problema (é uma LINHA, chevron + rótulo + slot à direita, para topos com uma segunda ação). Segue em uso no produto padrão (voltar + favoritar) e em `/catalog`. Os dois convivem de propósito.
- **Ficaram de fora, por não serem "só voltar":** `product/[id].tsx` (as duas variantes têm favoritar ao lado) e `quiz.tsx` (chevron + "Pular"). `checkout.tsx` e `reviews/[id].tsx` usam header NATIVO, sem botão custom.
- **typecheck + eslint + 57 testes OK.** Validado no simulador iOS 26 nas duas variantes (`/loyalty` claro, `/vip` escuro) — sem o "‹ Voltar" o título grande da tela deixa de competir com o canto.

### FAQ em acordeão + cadastro de cartão em flip card (2026-07-30)

Pedido do usuário, olhando os atalhos do Perfil que ainda não faziam nada: uma seção de **perguntas frequentes** com o [accordion do reacticx](https://www.reacticx.com/docs/components/accordion) e, na área de **formas de pagamento**, o [flip-card do reacticx](https://www.reacticx.com/docs/components/flip-card) para quando a pessoa vai adicionar um cartão. Terceiro e quarto portes do reacticx no projeto (depois de `AnimatedHeaderScrollView` e `ParallaxHeaderScrollView`).

- [x] **`src/components/Accordion/`** (`index.tsx` + `presets.ts` + `types.ts`) — compound como o original (`Accordion` / `.Item` / `.Trigger` / `.Content`). Divergências do porte, todas documentadas no cabeçalho do componente: **(1)** cor em **tokens** (`appearance`, presets `light`/`dark` em `presets.ts`), não hex cru num objeto `theme` — é o que deixa o mesmo componente servir creme e bordô; **(2)** o conteúdo é renderizado **uma vez** — o original desenha os filhos em DOBRO (uma cópia invisível só para medir a altura), aqui ele mora absoluto dentro da caixa de altura animada, já tem altura natural e se mede sozinho mesmo fechado (de graça, remedir passa a funcionar quando o texto muda; no original ficava travado no 1º layout); **(3)** **sem `BlurView`** sobre o conteúdo — o blur escuro do original vira um borrão cinza no meio da tela creme, e altura + opacidade contam a mesma história; **(4)** **sem háptico** — `expo-haptics` é módulo nativo fora do projeto (exigiria rebuild do dev client); **(5)** indicador **`plus`** no lugar do `cross` do original (que é um hambúrguer de 3 barras colapsando numa só, e não se lê como abrir/fechar).
  - **`spacing` mudou de sentido**: `0` (default) → UMA caixa com itens divididos por linha; `> 0` → cada item vira seu próprio card, **sem** divisória. No original as divisórias ficam nos dois casos, e aí a linha de baixo de um card encosta na borda do card seguinte (duas bordas coladas).
  - A11y: `accessibilityState={{ expanded }}` no gatilho e conteúdo fechado escondido de leitor de tela (`accessibilityElementsHidden` + `importantForAccessibility`) — fechado ele continua montado, então sem isso o leitor lê resposta de pergunta que não está aberta.
  - `Accordion.Trigger` com filho **string** ganha a tipografia de item de lista automaticamente; qualquer outro nó passa intacto.
  - **`Icon` novo: `chevronDown`** (viewBox deitado 14×8, não o `chevronRight` girado — o Svg é quadrado e um viewBox alto centralizado deixaria a seta pequena e fora do eixo do texto).
- [x] **`src/components/FlipCard/`** (`index.tsx` + `types.ts`) — compound (`FlipCard` / `.Front` / `.Back` / `.Trigger`), rotação em `rotateY` com `perspective: 1000`, troca de face por **opacidade em 90° além do `backfaceVisibility`** (no Android o backface sozinho não é confiável e as duas faces aparecem juntas no meio da virada). Divergências:
  - **Modo CONTROLADO (`flipped`)** — não existe no original e é o que sustenta a tela: a face de trás é decidida pelo FOCO do campo do CVV, não pelo toque. Controlado e não-controlado passam pelo MESMO efeito (`useEffect` observando `isFlipped`), então não há dois caminhos de animação.
  - **Sem `@sbaiahmed1/react-native-blur`** (borrão de movimento durante a virada): lib nativa de terceiros fora do projeto, efeito de ~200ms no meio de uma rotação que já esconde a face. `expo-blur`, que o projeto tem, não expõe `blurAmount` por face.
  - **⚠️ Sombra em DUAS camadas (não reintroduzir num nó só):** `overflow: 'hidden'` liga o `masksToBounds` da layer no iOS e a máscara recorta também a SOMBRA — o original põe `overflow`, `borderRadius` e `shadow*` no MESMO `Animated.View`, e o resultado é cartão sem sombra nenhuma. Aqui a de fora carrega transform/opacidade/sombra e a de dentro o raio + recorte (o gradiente tem de terminar no canto arredondado). O `style` de `.Front`/`.Back` vai na de DENTRO, para a borda dourada acompanhar o raio.
  - Sombra só no **iOS**: no Android a `elevation` de uma view que gira em `rotateY` é desenhada a partir da caixa não girada e fica uma mancha parada por baixo enquanto o cartão vira.
  - **`react-hooks/immutability`**: escrever num shared value dentro de `useCallback` (ou de qualquer hook que o receba por dependência) é erro de lint. Por isso `pressIn`/`pressOut` são funções simples **na raiz** (onde o `scale` é criado) e viajam pelo contexto, e o contexto **não** usa `useMemo`.
- [x] **`app/payment-methods.tsx`** — push da Stack raiz (fora do `VISIBLE_ON` da `TabBar`, então tela cheia), header igual a `/loyalty` e `/notifications` (`AnimatedHeaderScrollView` + `BackButton` no `leftComponent`, `useGoBack('/profile')`). Lista de cartões salvos (toque define o principal, "Remover" nos demais), estado vazio e o botão que leva a `/add-card`.
  - **⚠️ Título "Pagamento", não "Formas de pagamento"** (ajuste pedido pelo usuário depois de ver a tela): o crescimento do título grande no overscroll é limitado ao que couber em UMA linha, então título que já **nasce quebrado em duas** fica sem efeito nenhum e ainda come a altura de duas linhas na barra. Regra que vale para qualquer tela nova com `largeTitle`: **caber em uma linha na largura da tela**, ~12 caracteres no corpo 44. A linha do Perfil continua "Formas de pagamento" — ela nomeia o assunto, o título nomeia a tela.
  - Selo da bandeira usa **`CARD_BRAND_SHORT`**: "MASTERCARD" em caixa alta não cabe nos 46px do selo e quebrava em "MASTERC / ARD".
- [x] **`app/add-card.tsx`** — o cadastro é **tela própria** (pedido do usuário; nasceu como formulário que expandia dentro da lista). É uma tarefa com começo e fim: cartão pela metade não convive com a lista de cartões salvos, e o voltar já é o "cancelar" — o botão "Cancelar" que o formulário inline precisava deixou de existir. **Push normal, não modal**: no iOS uma rota apresentada como modal contamina as próximas da pilha (armadilha já documentada em `_layout.tsx`), e assim o header é o mesmo de todas as outras telas. Título "Novo cartão".
  - **Por que o cadastro é um flip card, e não enfeite:** o CVV é o único dado que não está na frente do cartão, e todo mundo já virou um cartão na mão para achá-lo. A face de trás **é a instrução** — tarja, faixa de assinatura e o número à direita dela. Focar o campo vira o cartão; o toque no cartão também vira, e aí o `onFlip` **tem de dar `blur()` no CVV**: sem isso o campo focado puxa a face de trás de volta no mesmo instante e o cartão parece travado.
  - A face mostra o número com **máscara completa desde o primeiro dígito** (`cardNumberPreview`) — a face não pode encolher e crescer a cada tecla.
- [x] **`src/store/useCardsStore.ts`** — `cards`/`primaryId` + `addCard`/`removeCard`/`setPrimary`. Nasceu junto com `/add-card`: com a lista guardando os cartões em `useState`, o cartão novo morria no `router.back()`, e passar por parâmetro de rota seria serializar objeto de domínio na URL só para atravessar uma tela. O **`id` é gerado no store**, não em quem chama, para a tela do formulário não inventar identidade (na Fase 16 quem devolve o id é o backend, e só este arquivo muda). Semente = `SAVED_CARDS`; não persistido, como os outros stores exceto `useUserStore`.
- [x] **`src/utils/card.ts`** — bandeira, agrupamento e máscara (lógica pura, sem `Date`, testável). **Elo antes de Visa/Mastercard**: os prefixos colidem (4011 é um "4", 5041 é um "50"), e sem essa ordem cartão Elo brasileiro é sempre rotulado errado. Amex muda três coisas ao mesmo tempo (15 dígitos, grupos 4-6-5, CVV de 4) — tudo derivado de `GROUPS`. Validade valida só a FORMA (mês 01–12); vencido/Luhn/BIN é backend na Fase 16. **16 testes novos (73 no total).**
- [x] **`src/data/faq.ts`** (`FAQ` — 6 perguntas com `id` estável, que um dia vira âncora de deep link) e **`src/data/paymentMethods.ts`** (`SAVED_CARDS`). O mock de cartão guarda **só** bandeira/últimos 4/titular/validade — número cheio e CVV não existem depois do cadastro, e um campo `number` no mock viraria um campo `number` no store da Fase 16.
- [x] **`AnimatedHeaderScrollView` ganhou teclado**: `keyboardShouldPersistTaps="handled"` + `keyboardDismissMode="on-drag"` (mesmos do `Screen` — sem `handled`, o primeiro toque no botão só fecha o teclado) e a prop **`adjustsForKeyboard`** (`automaticallyAdjustKeyboardInsets`, iOS), **desligada por padrão**: onde o teclado é aberto por uma barra de busca fixa (`/search`) ela brigaria com o inset que a própria barra já reserva. Ligada em `/add-card`.
- [x] **Atalhos do Perfil**: `LINKS` virou `{ label, route? }`. Sem `route` a linha continua sendo só texto, de propósito — linha que parece botão e não navega é pior que linha que nunca se ofereceu ao toque. **`Dados pessoais` e `Endereços salvos` seguem sem tela** (não estavam no pedido).
- **typecheck + eslint + 73 testes + export iOS OK.** Ainda **não** validado no simulador.

## 5. Domínios & features implementados

Nenhum domínio/feature de negócio ainda. Base técnica + design system prontos:
- `src/theme/*` — tema restyle completo e tipado.
- **`src/components/` (design system, todos apresentacionais e via barrel `@components/index`):**
  - Primitivos: `Box`/`TouchableOpacityBox`/`PressableBox`, `Text`.
  - Infra: `AppProviders`, `AnimatedSplash` (Lottie + fade), `Screen` (safe area + fundo sólido/gradiente + scroll + footer).
  - **`CurationBlock`** — a mesma peça visual em `variant="card"` (Home) e `variant="fullscreen"` (fundo de `/curation/[id]`), com **shared element transition implementada**: separa **forma** (gradiente/raio/geometria, `nativeID`=`transitionId`, `collapsable={false}`) de **conteúdo** (irmão com `opacity` própria). O card mede a si mesmo (`measureInWindow`) e grava o retângulo em `useTransitionStore` — **toda** saída para a tela cheia (toque no bloco e no CTA) passa por `openWithMeasure`, senão não há origem e o destino aparece de salto. O destino anima a forma dali até a janela inteira (reanimated, `.set()`/`.get()`), **morfando um único bloco de texto** (sai da posição/escala do texto do card e chega na do destino — as medidas da tela cheia são as do card × `FULLSCREEN_SCALE`, o mesmo fator em corpo/entrelinha/tracking/largura, o que mantém as quebras de linha idênticas; com dois blocos em crossfade o olho percebia que eram textos diferentes). O CTA, único elemento sem par no destino, acompanha a forma e sai em fade até 28%; o resto entra a partir de 45% e um scrim escurecendo a Home; voltar roda o inverso e só então navega (header + botão físico do Android). Conteúdo/cores vêm de `CURATIONS` (`@data`). Em tela cheia: status bar `light` + visível (full bleed), tab bar escondida (rota fora de `(tabs)`).
  - **`ParallaxHeaderScrollView`** — hero fotográfico com parallax (porte do reacticx), usado só em `/reserved`. Irmão do `AnimatedHeaderScrollView`: mesma família de barra fixa (gradiente + blur + máscara eased), mas o herói é a FOTO e não o título. Detalhes e desvios do original na seção "Coleção reservada" acima.
  - **`RareWineCard`** — ficha editorial de rótulo raro (nome em largura cheia, assinatura, `SpecChip` de safra/nota, pill de estoque com `Blip`, garrafa em nicho retroiluminado, rodapé com vídeo + preço). Só `/reserved` usa; é a variante "ler uma a uma" oposta ao `WineRow` ("varrer lista").
  - **`AnimatedHeaderScrollView`** ganhou **`leftComponent`** — o voltar de telas empilhadas, espelho do `rightComponent`. Com ele o título grande nasce abaixo da faixa da nav bar (senão o botão ficaria sobre a primeira linha) e o colapso inteiro é adiado por `collapseOffset` (= `HEADER_HEIGHT`), então a cadência da animação continua idêntica à das abas. Sem a prop, as contas são exatamente as de antes (offset 0).
  - **`FadeReentry` + `Reappear`** — contrapartida da transição acima na VOLTA. Como `/curation/[id]` é um push com `animation:'none'`, a Stack também não tem animação de pop: a forma encolhia macia até o card e todo o resto da Home reaparecia seco, de um frame para o outro. `FadeReentry` (provider, recebe o `transitionId`) publica um **relógio em ms** (shared value) e cada `Reappear order={n}` deriva dali sua janela (`n × 55ms` de atraso, 280ms de fade, 6px de subida, ease-out cúbico) — o provider não precisa saber quantos filhos tem. Gatilho: `openWithMeasure` grava `reentries[transitionId]` em `useTransitionStore` junto com a medida (mesma regra: toda saída passa por lá); o provider **zera a opacidade no BLUR** (já escondido sob o destino — zerar no foco arriscaria um frame em opacidade cheia) e anima ao reganhar o foco, consumindo a flag. A flag é o que restringe o fade à volta da tela cheia: as outras rotas (produto, busca, abas) têm animação de Stack própria e aparecem por baixo durante o gesto, onde zerar opacidade seria uma piscada. O **card fica fora** de `Reappear` — a forma aterrissa sobre ele, então precisa estar opaco no 1º frame. Sem provider, `Reappear` é um wrapper inerte.
  - **`organisms/wine-carousel`** (`WineCarousel` + `WineBackdrop` + `useCarouselProgress`) — coleção da curadoria como carrossel horizontal com snap (um vinho por vez, garrafa grande + legenda) e fundo que assume a tonalidade do vinho em foco. Os dois dividem um `progress` (shared value) criado pela tela; ele é normalizado em unidades de item no handler de scroll, então slides e fundo só raciocinam com índices. A tinta vem da cor do vidro com **alpha corrigido por luminância** (`conf.ts`) — sem isso um branco/espumante clarearia o bordô em vez de só sugerir a tonalidade. `WineBackdrop` entra como `extraBackground` do `CurationBlock`, ou seja DENTRO da forma, para ser recortado por ela durante a transição. Altura do slide é **medida** (`onLayout`), não fração da tela — com fração a legenda era cortada.
  - **`Accordion`** (`/.Item`/`.Trigger`/`.Content`) — porte do reacticx; `appearance` em tokens (presets `light`/`dark`), indicador `chevron` ou `plus`, `type` single/multiple, `spacing` (0 = caixa única com divisórias; >0 = um card por item). Usado na seção de perguntas frequentes do Perfil. Desvios do original na seção "FAQ em acordeão" acima.
  - **`FlipCard`** (`/.Front`/`.Back`/`.Trigger`) — porte do reacticx; virada em `rotateY` com modo **controlado** (`flipped`), sombra em duas camadas (⚠️ ver a armadilha do `overflow: hidden` acima). Usado no cadastro de cartão em `/payment-methods`.
  - Marca: `Icon` (registry: search/heart/bell/gift/bag/home/profile/chevronLeft·Right·Down/arrowRight/play/star/phone/check/plus), `StarRating` (leitura/editável), `BottleGraphic` (garrafa procedural; props `width/cor/capColor/showCap/premium/labelMode('initials'|'full')/iniciais/safra`), `Button` (primary/outline/outlineGold), `Pill`, `Chip`, `SectionTitle`, `Flag` (bandeiras SVG), `WineCard` (+`WineCardData`), `WineCountryCard` (atalho por país da Home), `WineRow` (light/dark, +`WineRowData`, `badge`/`subtitle`/`rightSlot`), `SegmentedToggle` (genérico), `Toggle`, `Toast`, `ScreenHeader` (light/dark — linha com chevron+rótulo, para topos com segunda ação), **`BackButton`** (light/dark — círculo de 36, o padrão das telas cujo topo tem só o voltar).
- `src/hooks/{useAppTheme,useAppFonts}.ts`.
- `src/config/env*.ts` — env validado (API_URL opcional até a Fase 16).
- **`src/data/`** (barrel `@data`): `types.ts` (Wine/Review/QuizQuestion/Occasion), `wines.ts` (WINES, 10), `reviews.ts` (REVIEWS), `quiz.ts` (QUIZ, 3), `occasions.ts` (OCCASIONS, 4), `curations.ts` (`CURATIONS`/`WEEKLY_CURATION`/`findCuration` — fonte única do conteúdo do `CurationBlock` nos dois estados), `collections.ts` (`RESERVED_COLLECTION` — fonte única da cópia da coleção reservada, no card da Home e em `/reserved`), `selectors.ts` (`findWine`, `railSelected`, `railBestSellers`, `specials`, `specialsSummary`, `countByType`, `countriesWithCount`, `winesByIds`, `searchWines`, `searchByDish`, `cartCount`, `cartSubtotal`, `CAT_SPECIALS`), `notifications.ts` (`NOTIFICATIONS` + tipos `AppNotification`/`NotificationKind`/`NotificationTarget`/`NotificationGroup`, `NOTIFICATION_GROUPS` e os seletores de não lidas/seção — ficam aqui, e não em `selectors.ts`, para não criar ciclo de import), `faq.ts` (`FAQ` — perguntas frequentes do Perfil), `paymentMethods.ts` (`SAVED_CARDS` — só bandeira/últimos 4/titular/validade).
- **`src/utils/`** (barrel `@utils`): `format.ts` (`brl`/`nf`), `pricing.ts` (`pointsDiscount`/`frete`/`checkoutTotal` + constantes), `wineViewModel.ts` (`toWineCardData`/`toWineRowData`/`toRareWineCardData`/`tipoUva`/`categoriaCompleta`/`capColorFor`), `card.ts` (bandeira/agrupamento/máscara de cartão — `cardBrand`/`formatCardNumber`/`cardNumberPreview`/`formatExpiry`/`cardCvvLength` + os `is*Complete`).
- **`src/store/`** (barrel `@store`, zustand): `useCartStore` (items + addToCart/setQty/removeFromCart/clear), `useFavoritesStore` (favs + toggleFav/isFav; inicia com lumiere-blanche+corona-reale), `useUserStore` (paladar/points/setPaladar), `useToastStore` (message + show(auto-dismiss ~2,2s)/hide), `useNotificationsStore` (`read` + markRead/markAllRead/isRead; feed é estático em `@data`, aqui só o estado de leitura), `useCardsStore` (`cards`/`primaryId` + addCard/removeCard/setPrimary; existe porque `/payment-methods` e `/add-card` são telas diferentes e precisam da mesma lista).
- `src/**/__tests__/` — 73 testes de lógica pura (format, pricing, selectors, filters, notifications, card).
- **Navegação:** `src/components/TabBar.tsx` (overlay flutuante **só Android**, lê `usePathname`, `VISIBLE_ON`=as 5 abas/`ACTIVE_TAB`, badge via `useCartStore`); no **iOS** a barra é nativa (`app/(tabs)/_layout.tsx`). `ToastHost.tsx` consome `useToastStore`. `src/theme/navHeader.ts` = opções de header nativo de marca.
- **Rotas `app/`:** `_layout.tsx` (Stack raiz `headerShown:false` + `{Platform.OS!=='ios' && <TabBar/>}` + `<ToastHost/>` + `<AnimatedSplash/>`), `index.tsx` (aguarda hidratação → `onboarded ? /home : /quiz`), `quiz.tsx`. **Grupo `(tabs)/`**: `_layout.tsx` (iOS `<NativeTabs>` / Android `<Stack>`), e `home|search|favorites|bag|profile/{_layout.tsx (Stack, header nativo),index.tsx}`. **Pushes raiz (tela cheia):** `sommelier.tsx`, `product/[id].tsx`, `reviews/[id].tsx`, `checkout.tsx`, `tracking.tsx`, `loyalty.tsx`, `vip.tsx`, `notifications.tsx` (central de notificações — destino do ícone `bell` no header da Home), `reserved.tsx` (coleção reservada — destino do card "Vinhos raros & especiais" da Home e das notificações de curadoria; hero fotográfico em parallax + fichas `RareWineCard`), `payment-methods.tsx` (pagamento — destino do atalho do Perfil; cartões salvos), `add-card.tsx` (cadastro de cartão com o `FlipCard`), `curation/[id].tsx` (destino do bloco de curadoria — usa `CurationBlock variant="fullscreen"` como fundo, sem recriar o bordô; compõe o carrossel + o fundo tonal, é ela que cria o `progress` compartilhado), `catalog.tsx` (revisão do DS — remover antes de publicar). URLs seguem iguais (grupos são transparentes: `/home`, `/search`…).

## 6. Pendências & próximos passos

**MVP + refinamento nativo completos.** O que resta é opcional.

0. **⚠️ Requer DEV BUILD (não Expo Go):** native-tabs (`expo-router/unstable-native-tabs`), `@expo/ui` e AsyncStorage são módulos nativos. Rodar com `npx expo run:ios` / `npx expo run:android` (há `expo-dev-client`). Num binário antigo dariam "módulo ausente".
1. **Calibrar no device (iOS+Android):** SF Symbols vs. ícones do design nas abas; cor/contraste do `SegmentedControl` e do large title; **Lottie da splash sobre o novo fundo creme** (foi desenhado p/ fundo escuro — pode precisar de ajuste); `paddingBottom="s108"` das telas de aba talvez sobre no iOS (native tabs já aplica inset via `contentInsetAdjustmentBehavior`); timing de Blip/PulseBar.
2. **Antes de publicar:** remover a rota `/catalog` (revisão do DS); revisar `bundleIdentifier` (`com.ydivino`) e `scheme` (`yydivinomobile`).
3. **a11y (aprofundar, se desejado):** varredura completa de `accessibilityRole`/labels e contraste.
4. **Fase 16 (backend):** quando houver API, criar os domínios (`Api/Adapter/Service/useCases` react-query) e trocar os mocks, **mantendo** os seletores (`@data`) e mappers (`@utils`) — a UI não muda. Usar a skill `sync-backend` para alinhar contratos.
5. **Performance da transição (decisões deliberadas — não desfazer sem medir):** durante a abertura, o único nó que relayouta é a forma; posição é `transform`, não `left`/`top`; o conteúdo pesado (garrafas + tintas) monta só em `onOpenComplete`; `extraBackground` tem tamanho fixo dentro da forma. Medido no simulador: gap máximo da thread JS **durante** a animação caiu de 55ms para 20–25ms, e o stall do mount (~65ms) foi realocado para depois do movimento. Próximas alavancas, se um aparelho fraco ainda sofrer: (a) trocar o `overflow:hidden`+raio da forma por raio pintado no próprio gradiente, evitando o clip arredondado por frame no Android; (b) em coleções grandes, montar só uma janela de camadas de tinta / migrar o carrossel para `Animated.FlatList` virtualizada.
6. **Shared element da curadoria — calibrar no device:** durações (`OPEN_DURATION` 560ms / `CLOSE_DURATION` 420ms), curva (`CURVE`), pontos de crossfade (`GHOST_FADE_END` 0.28 / `CONTENT_FADE_START` 0.45) e `OPACIDADE_SCRIM` em `CurationBlock.tsx`. Na volta, calibrar também a reentrada da Home em `FadeReentry.tsx` (`ITEM_DURATION` 280ms / `STEP` 55ms / `OFFSET` 6px) — deve ser quase imperceptível, o protagonista continua sendo o card. Ainda **não** entra em fade a `TabBar` custom do Android (overlay do root, montado/desmontado por rota): no iOS a barra é nativa e reaparece com o pop, então fadear só a do Android criaria divergência — reavaliar no device. Validado no simulador iOS (frame a frame). Conferir no Android se o `measureInWindow` bate com a janela edge-to-edge (a forma deve nascer exatamente sobre o card, sem salto).
7. Persistência de **onboarding já feita** (AsyncStorage). Falta persistir carrinho/favoritos, se desejado (mesmo padrão `persist`).
8. **`/reserved` — calibrar no device (iOS+Android):** `IMAGE_HEIGHT` (380) é um compromisso entre recorte da foto e espaço para o título — em telas pequenas (SE) o hero come 60% da altura; `PARALLAX_FACTOR` (0.5) e a janela `OVERLAY_FADE_START/END` (0.25/0.7). **No Android o overscroll não existe** (glow em vez de bounce), então o crescimento da foto ao puxar só acontece no iOS — o parallax ao rolar funciona nos dois. Conferir também o blur da barra fixa (`MAX_BLUR_INTENSITY` 30) com `dimezisBlurViewSdk31Plus` em Android < 31, onde cai para view semitransparente.

9. **FAQ + `/payment-methods` + `/add-card` — validar no simulador (ainda não foi):** abrir/fechar do acordeão (a altura é medida, então checar pergunta que quebra em 2 linhas), a virada do cartão ao focar o CVV **com o teclado aberto** (é o que `adjustsForKeyboard` resolve, e é iOS-only — no Android conferir se o campo não fica coberto), e a sombra do cartão sobre o creme. Faltam as telas de **`Dados pessoais`** e **`Endereços salvos`** (os dois atalhos restantes do Perfil seguem como texto, sem `route`).

## 7. Notas / armadilhas

- **Só variáveis `EXPO_PUBLIC_*`** chegam ao bundle. `env.API_URL` é opcional agora (falha cedo se inválida).
- **Reanimated 4**: o plugin correto no `babel.config.js` é `react-native-worklets/plugin` (deve ser o último). `runOnJS` está **deprecado** → usar `scheduleOnRN` de `react-native-worklets`. E preferir `sv.get()` / `sv.set()` a `sv.value` — o eslint novo (`react-hooks/immutability`) barra a atribuição em `.value` quando o shared value já foi passado a outro hook.
- **Shared element (curadoria)**: a rota `curation/[id]` é declarada no `app/_layout.tsx` como push **normal** (`card`) + `animation: 'none'` + `contentStyle` no creme da Home + `gestureEnabled: false`. **Não usar `transparentModal`** (nem `containedTransparentModal`): mantinha a Home viva por baixo, mas no iOS toda tela empilhada depois de um modal também vira modal — a tela de produto aberta da coleção subia de baixo como sheet. `animation: 'fade'` também não serve (expõe Home e destino ao mesmo tempo, com o texto do card em dobro). O preço da solução atual é que o entorno da forma durante o crescimento é só a cor de fundo da Home, não o conteúdo dela; por isso o scrim foi removido (não há mais o que escurecer).
- **letterSpacing**: o design usa `em`; no RN é **px**. Já convertido nos `textVariants` (aproximação por variante); ajustar caso destoe.
- **Garrafas**: são formas desenhadas (não imagens). Props previstas: `cor`, `cap`(dourado se premium), `iniciais`, `safra`, e um `size` com presets (rail 46×150, lista 34×100, produto 96–110×320–340, sacola 30×92).
- **Navegação "voltar"**: o protótipo usa pilha própria (`prev[]`); no app usar a stack nativa do Expo Router.
- **Tab bar**: **iOS = Native Tabs** (`(tabs)/_layout.tsx`, split por `Platform`); **Android = TabBar flutuante custom** (overlay do root, visível só nas 5 abas). Telas secundárias (sommelier/loyalty/vip) e fluxos (checkout/tracking) abrem em tela cheia.
- **Native Tabs / `@expo/ui` / AsyncStorage = nativos → dev build.** Ícones das abas usam SF Symbols no iOS; validar API do pacote instalado antes de mexer (feito nesta rodada).
- **Header nativo**: `brandHeaderOptions` + `Screen nativeHeader`. Large title só colapsa no iOS e exige `ScrollView` com `contentInsetAdjustmentBehavior="automatic"` (a prop `nativeHeader` já faz isso). Telas escuras imersivas ficam sem header nativo de propósito.
- TS 6 + Expo: `tsc --noEmit` é o typecheck; Metro resolve os aliases do tsconfig automaticamente.

---

# Referência de Design (fonte da verdade — não apagar)

## Paleta (→ `src/theme/palette.ts` e cores semânticas em `theme.ts`)

| Semântico (theme) | Hex | Uso |
|---|---|---|
| `background` | `#F3ECDD` | Fundo telas claras |
| `surface` | `#FBF7EE` | Cards, inputs, linhas |
| `backgroundOuter` | `#E7E0D2` | Fundo externo |
| `toggleTrack` | `#EADFCE` | Trilho de toggles |
| `mapBackground` | `#E4DDCD` | Fundo do mapa |
| `primary` | `#431018` | Bordô principal (texto/botões/ícones no claro) |
| `primaryLight` | `#5A1622` | Topo de gradientes radiais |
| `primaryAlt` | `#3A0E18` | Gradiente do quiz |
| `primaryDeep` | `#2C0A10` | Fundo dark profundo |
| `primaryDeeper` | `#320B12` | Variação mais escura |
| `accent` | `#B08D57` | Dourado (acento, bordas, labels, estrelas) |
| `accentDark` | `#8A6C40` | Dourado escuro / links |
| `textPrimary` | `#2A211C` | Texto sobre creme |
| `textOnDark` | `#F3ECDD` | Texto sobre fundo escuro |
| `mutedIcon` | `#8A7D70` | Ícone de busca |

Transparências recorrentes mapeadas em `alpha` (ex.: `inkA50/55/60`, `cremeA*`, `goldA*`, `wineA*`). Fundos dark = `radial-gradient(120% 90% at 50% 18%, #5A1622, #431018 45%, #2C0A10 100%)` e variações (usar `expo-linear-gradient`).

## Tipografia
- **Cormorant Garamond** (serif): títulos, nomes de vinho, preços, citações itálicas. Variantes: `brand`(58), `h1`(44), `h2`(34), `sectionTitle`(25), `wineName`(20), `wineNameSm`(16), `price`, `quote`(italic).
- **Jost** (sans): UI/labels/corpo. Variantes: `body`(14), `bodySm`(12), `label`(11 caps), `eyebrow`(9 caps dourado), `button`(12 caps).

## Espaçamento & forma
- Padding horizontal padrão **22** (`s22`); topo **56** (`s56`, notch); rodapé tab **~108** (`s108`). Radii: chips/pills `r8/r9`, cards `r12–r16`, tab bar `r20`, botões `r6–r11`. Device ref: **402×874**.

## Modelo de dados (Fase 2 — copiar valores exatos do HTML)

`Wine` = `{ id, nome, safra, tipo:'Tinto'|'Branco'|'Rosé'|'Espumante', uva, regiao, assinatura, preco:number, destaque:boolean, notaMedia, totalAvaliacoes, estoqueBaixo:boolean, harmonizacoes:string[], cor:hex, iniciais, videoDur? }`.
**10 vinhos** (`this.wines`, linhas 980–991): Notte Eterna*, Perla Nera*, Corona Reale*, Aurora del Sud, Lumière Blanche, Rosa dei Venti, Sangue di Terra, Fiore d'Inverno, Velluto Rosso, Alba Serena. (* = `destaque`.)
`Review` = `{ nome, nota, comentario }` mapeado por wineId (linhas 992–1010).
`QuizQuestion` (3): estilo(seco/suave), corpo(encorpado/leve), momento(jantar/solo) (linhas 1011–1021).
`Occasion` (sommelier, 4): romântico, presente, churrasco, comemoração → `ids[]` (linhas 1022–1027).
Estado global (zustand): `cart:{[id]:qty}`, `favs:{[id]:true}` (inicia com lumiere-blanche + corona-reale), `paladar` (default 'encorpado'), `points:320`.

### Regras de negócio
- `brl(n)`→`"R$ "+n.toLocaleString('pt-BR')`; `nf(n)`→1 casa com vírgula.
- Rail "Selecionados": `tipo==='Tinto' || destaque`, 5 primeiros. Rail "Mais vendidos": sort `totalAvaliacoes` desc, top 4. Especiais/VIP/Curadoria: `destaque===true`.
- Busca por prato: casa `dishQuery` com `harmonizacoes`.
- `buyFromProduct`: se `destaque && estoqueBaixo` → **reservar** (toast 24h); senão add à sacola + vai p/ sacola.
- Fidelidade: 320 pts, meta 500 → 64%; 320 pts = R$ 64 desconto. Frete: grátis > R$ 300, senão R$ 29. Total = `max(0, subtotal − desconto) + frete`. `placeOrder` limpa carrinho/gift/pontos → tracking.

## Mapa de telas & navegação
```
Splash(Lottie) → index (hidrata) → 1º acesso: Quiz → Home · demais: Home direto
Home ├─ Buscar (vinho/prato/filtros) ├─ Sommelier ├─ Favoritos └─ Curadoria/Especiais
        → Produto (Padrão destaque:false / Premium destaque:true → vídeo+reserva) → Avaliações
Sacola → Checkout+Gifting → Pedido feito → Acompanhamento (Status·Mapa)
Perfil ├─ Fidelidade └─ Acesso VIP
```
Tab bar (5): Início · Buscar · Favoritos · Sacola(badge) · Perfil.
Estrutura de rotas planejada (Fase 3): `app/index`(splash), `app/quiz`, `app/(tabs)/{home,search,favorites,bag,profile}`, `app/sommelier`, `app/product/[id]`, `app/reviews/[id]`, `app/checkout`, `app/tracking`, `app/loyalty`, `app/vip`.

## Referências no repositório
- Design: `design/project/IL DiVino.dc.html` (estilos por tela 32–962; lógica/dados 967–1269). Screenshots: `design/project/shots/`.
- Infra do protótipo (ignorar p/ produção): `support.js`, `ios-frame.jsx`.
- Regras do projeto: `AGENTS.md` (ler docs Expo v57 antes de codar).

## Checklist de telas (15)
- [x] Splash · [x] Quiz · [x] Home · [x] Busca · [x] Sommelier · [x] Produto Padrão · [x] Produto Premium · [x] Avaliações · [x] Sacola · [x] Checkout+Gifting · [x] Acompanhamento · [x] Favoritos · [x] Perfil · [x] Fidelidade · [x] VIP  ← **todas as 15 telas prontas**
